package serialization;

import java.io.ObjectOutputStream;

public class SeralizePerson {
	
	private filename = "src/main/resourses/person.txt";
	
	public void writePerson(Person p) {
		
		ObjectOutputStream 00s = new ObjectOutputStream(new FileOUt);
		try (ojectOutputStream oos = ObjectOutputStream(new FileOutputStream(Filenaem)
	}
public Person readPerson(Person p) {
	static void readObject(String filename) {
		try(ObjectInputStream ois = new ObjectInputStream)
		
	}
}
}
